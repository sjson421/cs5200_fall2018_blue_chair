(function() {
    angular
      .module("RestoFinder")
      .controller("HomeController", function(
        $rootScope,
        $scope,
      ) {
            $scope.testvar = "Angular Working Places";
      });
  })();
  